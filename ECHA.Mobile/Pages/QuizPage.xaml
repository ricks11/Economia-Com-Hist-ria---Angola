<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pageModels="clr-namespace:ECHA.Mobile.PageModels"
             xmlns:dto="clr-namespace:EconomiaComHistoria.Core.DTOs;assembly=ECHA.Core"
             x:Class="ECHA.Mobile.Pages.QuizPage"
             x:DataType="pageModels:QuizPageModel"
             Title="Quiz">

    <StackLayout Padding="20" Spacing="20">
        <ProgressBar Progress="{Binding Progress}" />
        <Label Text="{Binding SecondsRemaining}" HorizontalOptions="Center" />
        
        <Label Text="{Binding CurrentQuestion.Enunciado}" FontSize="20" />
        
        <CollectionView ItemsSource="{Binding CurrentQuestion.Opcoes}">
            <CollectionView.ItemTemplate>
                <DataTemplate x:CompileBindings="False">
                    <Button Text="{Binding Texto}" 
                            Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.AnswerQuestionCommand}"
                            CommandParameter="{Binding .}" />
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </StackLayout>
</ContentPage>
